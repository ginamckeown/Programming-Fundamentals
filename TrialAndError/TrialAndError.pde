import processing.sound.*;
SoundFile sound;

void setup() {
  size(640, 360);
  background(255);
  // by Greek555 on freesound.org
    sound = new SoundFile(this, "Unit2GameSound.mp3");

    
  // Load a soundfile from the /data folder of the sketch and play it back
}      

void draw() {
}
